import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.prayerhouse.projectjerusalem',
  appName: 'Project Jerusalem',
  webDir: 'build'
};

export default config;
